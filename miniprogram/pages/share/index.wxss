.share-page {
  padding-bottom: calc(env(safe-area-inset-bottom) + 160rpx);
}

.share-loading {
  min-height: 70vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 22rpx;
  color: var(--muted);
}

.loading-flower {
  width: 62rpx;
  height: 62rpx;
  background: var(--pink-soft);
  border: 12rpx solid var(--yellow);
  border-radius: 50%;
  animation: breathe 1s ease-in-out infinite alternate;
}

.share-body { padding: 0 4rpx; }

.share-heading {
  padding: 20rpx 12rpx 28rpx;
  text-align: center;
}

.share-kicker {
  color: var(--coral);
  font-family: Georgia, "Times New Roman", serif;
  font-size: 17rpx;
  letter-spacing: 0.18em;
}

.share-title {
  margin-top: 12rpx;
  font-family: "Kaiti SC", "STKaiti", "Songti SC", serif;
  font-size: 40rpx;
  font-weight: 700;
  line-height: 1.35;
  color: #3d3438;
}

.share-meta {
  margin-top: 12rpx;
  color: var(--muted);
  font-size: 20rpx;
}

.shared-results {
  display: flex;
  flex-direction: column;
  gap: 28rpx;
}

.shared-result {
  overflow: hidden;
  background: #fff;
  border: 2rpx solid var(--line);
  border-radius: 18rpx;
  box-shadow: 0 12rpx 30rpx rgba(110, 83, 94, 0.07);
}

.shared-result image {
  width: 100%;
  display: block;
}

.compare-list {
  display: flex;
  flex-direction: column;
  gap: 28rpx;
}

.compare-card {
  overflow: hidden;
  background: #fff;
  border: 2rpx solid var(--line);
  border-radius: 20rpx;
  box-shadow: 0 14rpx 32rpx rgba(110, 83, 94, 0.07);
}

.compare-hero {
  overflow: hidden;
  background: #f7f4f5;
}

.compare-hero__image {
  display: block;
  width: 100%;
}

.compare-origin {
  display: flex;
  align-items: center;
  gap: 16rpx;
  margin: 0;
  padding: 16rpx 18rpx 18rpx;
  background: linear-gradient(180deg, #fffaf8 0%, #fff 100%);
  border-top: 2rpx solid #f0e6e8;
}

.compare-origin--hover {
  background: #fff5f6;
}

.compare-origin__thumb {
  flex: none;
  width: 96rpx;
  height: 96rpx;
  border-radius: 14rpx;
  background: #efe8ea;
  box-shadow: 0 0 0 2rpx rgba(110, 83, 94, 0.06);
}

.compare-origin__copy {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 4rpx;
}

.compare-origin__label {
  color: #5a4f53;
  font-size: 24rpx;
  font-weight: 650;
}

.compare-origin__hint {
  color: var(--muted);
  font-size: 20rpx;
}

.compare-origin__arrow {
  flex: none;
  color: #c4b4b8;
  font-size: 36rpx;
  line-height: 1;
}

.share-note {
  margin-top: 28rpx;
  padding: 0 8rpx;
  text-align: center;
  color: var(--muted);
  font-size: 20rpx;
  line-height: 1.55;
}

.share-dock {
  position: fixed;
  right: 0;
  bottom: 0;
  left: 0;
  z-index: 20;
  padding: 18rpx 28rpx calc(env(safe-area-inset-bottom) + 18rpx);
  display: flex;
  align-items: center;
  gap: 16rpx;
  background: rgba(255, 249, 248, 0.96);
  border-top: 2rpx solid var(--line);
  box-shadow: 0 -10rpx 28rpx rgba(108, 80, 91, 0.06);
  box-sizing: border-box;
}

.share-dock button {
  flex: 1;
  height: 88rpx;
  margin: 0;
  padding: 0;
  border-radius: 999rpx;
  font-size: 28rpx;
  font-weight: 650;
  line-height: 88rpx;
}

.share-dock__save {
  background: #fff;
  border: 2rpx solid var(--line);
  color: var(--ink);
}

.share-dock__create {
  background: var(--coral);
  color: #fff;
  box-shadow: 0 10rpx 24rpx rgba(231, 109, 130, 0.22);
}

@keyframes breathe {
  from { transform: scale(0.92); opacity: 0.7; }
  to { transform: scale(1); opacity: 1; }
}
