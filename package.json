{
  "name": "huayang-miniapp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch server/src/index.mjs",
    "start": "node server/src/index.mjs",
    "test": "node --test server/test/*.test.mjs",
    "check": "node scripts/validate.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
